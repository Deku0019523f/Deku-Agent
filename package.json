{
  "name": "deku-agent",
  "version": "1.0.0",
  "description": "Agent de développement autonome pour Android/Termux — multi-provider LLM, boucle agentique, outils shell/fichiers.",
  "bin": {
    "deku": "./dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
